// Node-Brunch configuration file.
// Does nothing but pull the configuration from node-config; see the config/
//   directory for all the config values.

(function() {

  return exports.config = require('config').brunch;

}).call(this);
